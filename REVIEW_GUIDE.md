# Production quality pass review guide

> Generated from `review_contracts.json`. Edit the inventory, then run `node scripts/generate_review_guide.js`.

## Logical review map

| Domain | Files | Primary tests |
| --- | --- | --- |
| Night recovery and delayed Day notices | `game.js`<br>`runtime_night.js`<br>`production_mode_router.js`<br>`production_title_experience.js`<br>`production_wrapper_guard.js` | `test_night_recovery.js`<br>`test_day_notice_queue.js`<br>`test_production_compositor.js`<br>`scripts/quality_acceptance_browser.mjs` |
| Act IV investigation and canonical continuity | `campaign_act2.js`<br>`campaign_native_act2.js`<br>`campaign_visual_direction.js` | `test_campaign_act4_investigation.js`<br>`test_campaign_native_act2.js`<br>`scripts/act4_acceptance_browser.mjs` |
| Responsive Day and Night presentation | `style.css`<br>`campaign_native_act1.js`<br>`campaign_native_act1_visuals_impl.js`<br>`production_presentation_guard.js` | `test_production_presentation_guard.js`<br>`scripts/presentation_quality_browser.mjs` |
| Cinematic interruption and co-op entry | `good_dogs_cutscenes_v2_2.js`<br>`good_boys_crash_scene.js`<br>`good_dogs_home_scene.js`<br>`good_boys_earthfall_ending.js`<br>`good_boys_button_hard_fix.js` | `test_cinematic_presentation.js`<br>`test_crash_playback_budget.js`<br>`test_good_boys_intro_repair.js`<br>`test_good_dogs_coop.js`<br>`scripts/quality_acceptance_browser.mjs` |
| Orbital staging and shared HUD ownership | `cinematic_systems.js`<br>`orbital_scene_staging.js`<br>`good_boys_legacy_hud_filter.js`<br>`good_dogs_mobile_visual_polish.js` | `test_cinematic_systems.js`<br>`test_orbital_scene_staging.js`<br>`test_good_boys_ui_ownership.js`<br>`test_production_presentation_guard.js` |
| Build integration and release evidence | `index.html`<br>`bg_noc.js`<br>`production_bootstrap.js`<br>`campaign_native_act1_visuals.js`<br>`release_certification.json`<br>`scripts/quality_release_receipt.mjs`<br>`.github/workflows/merge-gate.yml` | `test_static_entrypoint_integrity.js`<br>`test_quality_release_receipt.mjs`<br>`scripts/production_release_gate.js` |

## Failure-mode contracts

| ID | Failure mode | Invariant | Executable test | Spot check |
| --- | --- | --- | --- | --- |
| `GD_CODEC_CAPABILITY` | An environment without an H.264 decoder attempts the authored movie and enters a retry or autoplay loop. | Playback eligibility is decided from MediaSource.isTypeSupported and HTMLMediaElement.canPlayType, never a user-agent string; unsupported environments explain that video is unavailable and retain an explicit Skip control. | `test_review_contracts.js#GD_CODEC_CAPABILITY` | Stub both capability probes as unsupported; confirm no media source is loaded, VIDEO UNAVAILABLE and the recovery explanation are visible, Skip is reachable, and campaign state is unchanged. |
| `GD_NO_INVISIBLE_ADVANCE` | Media failure or missing decode advances the campaign without a completed movie or deliberate skip. | Only COMPLETED or USER_SKIPPED may settle a cutscene; capability failure is non-terminal and cannot write watched state. | `test_review_contracts.js#GD_NO_INVISIBLE_ADVANCE` | Open a cutscene with both probes unsupported and verify its promise remains pending until the explicit SKIP control is used. |
| `GD_SKIP_IDEMPOTENT` | A double tap, keyboard event, or late ended event applies cutscene completion more than once. | The first terminal event owns settlement; every later skip or completion event is a no-op and produces no second state write. | `test_review_contracts.js#GD_SKIP_IDEMPOTENT` | Trigger skip twice and then emit ended; verify one result, one state write, and the same narrative state as a single skip. |
| `CANONICAL_STORY_NON_BYPASS` | A historical Felicia confrontation, boss, unlock, or day-ending route re-enters canonical Story Bible mode. | Canonical story mode fails closed across every historical route and Felicia becomes playable only after the ordered Act VI Duet reward. | `test_story_authority_firewall.js#canonical-firewall` | Load canonical mode with legacy Felicia save keys and call public legacy scene APIs; no retired scene, reward, or durable unlock may fire. |
| `MODE_SHELL_SINGLE_OWNER` | Day, Night, and Good Dogs retain duplicate presentation or input ownership after a handoff. | The production bootstrap installs one runtime mode shell and repeated enter/exit calls restore the exact owned surface and input snapshot once. | `test_runtime_mode_shell.js#single-owner` | Enter and exit Night twice from both title and Day; confirm one shell install, no held input, no duplicate blockers, and exact Day restoration. |
| `NIGHT_CHECKPOINT_RECOVERY` | An interrupted run loses its world, duplicates rewards, or silently starts fresh when restoration fails. | Validated standalone checkpoints restore the most recent world without changing campaign saves; failed restore stops at the title until an explicit action. Completion tombstones precede settlement. | `test_night_recovery.js` | Move, damage an enemy, travel, reload and Resume Night; compare saved route, health, rewards, enemies and clock. Inject storage failure and confirm visible retry without a fresh run. |
| `DAY_NOTICE_MODAL_OWNERSHIP` | A delayed procedural pager replaces an authored conversation or appears in a different run. | Day notices wait for the current modal to release and expire across changed run, day or standalone mode. | `test_day_notice_queue.js` | Keep an investigation open beyond the pager delay. Close it and receive the notice once; starting a different mode must cancel the stale notice. |
| `ACT4_HUMAN_VERIFICATION` | Selecting an approach immediately awards alliance, or technical recovery is mistaken for requester success. | Observations, supported hypothesis, bounded response, technical verification, requester confirmation and shared report occur in order. Evidence and Trust are awarded once at distinct boundaries. | `test_campaign_act4_investigation.js` | Try a wrong hypothesis, reload mid-investigation, then verify the service. Alliance must remain locked until Inspection confirms its task and the joint report is saved. |
| `VISIBLE_CINEMATIC_SETTLEMENT` | Background playback, stale input or a failed save settles an ending invisibly or twice. | The Good Dogs movie, home prologue and Earthfall owners prevent hidden progression and keep focus in the active modal. Earthfall retains visible save retry without repeating semantic completion; the crash movie has its separate executable contract below. | `test_cinematic_presentation.js` | Background the film, deliver ended, return and Resume. Double Skip and retry a failed Earthfall save; verify one terminal narrative transition. |
| `CRASH_CINEMATIC_RECOVERY` | A crash-movie error, stall watchdog or background fallback advances M2 without visible completion or deliberate Skip. | The playback watchdog allows the full remaining duration plus three seconds; healthy playback settles on ended. Capability failure and watchdogs expose recovery without settling; hidden completion waits for visible Resume or Skip, and terminal input is idempotent. | `test_crash_playback_budget.js` | Run the healthy 9.433-second movie to its end, then inject unsupported capability, stalled playback and hidden ended separately. Confirm visible recovery, trapped/restored focus and one completion only after deliberate Skip or visible Resume. |
| `DAY_SCENE_REACHABILITY` | Artwork or touch movement controls cover choices; stale typewriter handlers restore previous dialogue. | Authored art shares the dialogue stacking context, reserves a separate region, and every visible choice is a reachable 44px target; each conversation owns its text. | `scripts/presentation_quality_browser.mjs` | Open standup and workstation at320px portrait and844px landscape. Use each real choice, scroll if needed, and verify the correct scene copy persists. |
| `NIGHT_CONTROL_STARTUP` | Night controls appear in the wrong layout or shift beneath the player when visual decoration arrives after input becomes active. | Viewport and active input ownership determine control geometry from startup. Delayed decoration and MORE cannot relocate persistent targets, obscure actors, overlap controls or reduce the 44px targets. | `scripts/presentation_quality_browser.mjs` | At 568 by 320, delay only the Night decoration class, launch through real controls, expand MORE, then release decoration while expanded. Repeat six toggles and compare hit targets, actor clearance and persistent control bounds. |
| `GOOD_DOGS_HUD_SINGLE_OWNER` | A viewport timer alternately hides and reveals the authoritative Good Dogs HUD. | Only the cinematic guard controls the shared hide flag; viewport styling cannot suppress ordinary gameplay. Duplicate objective strips are filtered without removing combat or narrative messages. | `test_production_presentation_guard.js` | Observe portrait gameplay across repeated mobile/cinematic timer ticks, open and close a modal, and verify one stable HUD. |
| `GOOD_DOGS_CANCEL_REOPEN` | An enabled Good Dogs card swallows a rapid reopen after cancelling its selector. | Cancellation releases launch debounce and title ownership synchronously while duplicate events during a live launch still coalesce. | `test_good_boys_intro_repair.js` | Open the selector, cancel, immediately reopen, and choose Solo or Local. No artificial delay or second tap is needed. |
| `RELEASE_EVIDENCE_BOUNDARY` | Green fixtures are presented as physical-device, complete-route or licensed-decoder certification. | Release assessment requires matching candidate identity, intact artifact hashes and requirement-specific evidence. Missing physical, decoded-media, unassisted, soak or protection evidence blocks release. | `test_quality_release_receipt.mjs` | Run npm run release:assess with missing, stale, altered or incomplete reports. Each case must remain blocked; one clip cannot satisfy the full media manifest. |

## Deletion and retirement ledger

| ID | Removed or retired | Why | Resurrection check |
| --- | --- | --- | --- |
| `RETIRED_CRASH_PLATE_AUTO_COMPLETION` | The crash fallback plate's 2.2-second automatic completion timer. | A loaded image or elapsed hidden-tab time cannot stand in for visible movie completion or deliberate Skip. The approved plate remains as passive recovery artwork. | `test_crash_playback_budget.js` |
| `RETIRED_FELICIA_VILLAIN_ROUTES` | Early Felicia confrontation, secret-boss unlock, premature playable selection, and historical ending schedules in canonical mode. | They conflict with the Story Bible v1.2 trust-to-alliance arc and ordered Act VI Duet reward. | `test_story_authority_firewall.js#canonical-firewall` |
| `RETIRED_LEGACY_EARTHFALL_REPLAY` | Automatic b736m8 canvas finale replay from completed standalone Good Dogs resumes. | The authored Earthfall authority owns completion, persistence, replay, and return-to-title semantics. | `test_review_contracts.js#RETIRED_LEGACY_EARTHFALL_REPLAY` |
| `RETIRED_DUPLICATE_MODE_OWNERSHIP` | Independent mode handoff writes that left Day UI, held input, or ARIA blockers active beneath Night or Good Dogs. | One reversible mode shell must own presentation and input boundaries. | `test_review_contracts.js#RETIRED_DUPLICATE_MODE_OWNERSHIP` |
| `RETIRED_CANONICAL_RACKS_SHORTCUT` | Canonical Act IV invocation of the legacy racks movie and inherited legacy-choice bridge; historical noncanonical code and assets remain. | The legacy movie assigned an enemy and toolkit before observations. The authored native console now owns plan, evidence and human verification. | `test_campaign_native_act2.js` |
| `RETIRED_MOBILE_HUD_FLAG_WRITER` | Mobile visual timer writes to the shared Good Dogs hide-HUD flag. | They raced the cinematic guard and caused intermittent missing HUDs. Responsive style remains passive. | `test_production_presentation_guard.js` |

## Approval boundary

Do not merge until the named remote checks in the PR description are successful and every contract above has an executable test reference. Physical-device and licensed-browser media certification remain separate release gates.
