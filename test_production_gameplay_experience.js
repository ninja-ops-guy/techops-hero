'use strict';
const assert=require('assert'),fs=require('fs'),vm=require('vm');
const source=fs.readFileSync('production_gameplay_experience.js','utf8');
function boot(){let rafFn=null;const ctx={console,Math,Date,setTimeout,clearTimeout,requestAnimationFrame(fn){rafFn=fn;return 1;},cancelAnimationFrame(){},matchMedia(){return {matches:false};},navigator:{vibrate(){}},V67SET:{shake:true,volSfx:0},S:{nightMode:false,inBattle:false,inDialog:false},NM:null,TechOpsLevelRegistry:{goodDogsMission(m){return m===3?{id:'gooddogs.m3',ordinal:3,name:'BREACH',objective:'CROSS THE BREACH',target:1450,stage:{landmarks:[{x:330,label:'IMPACT',kind:'breach'},{x:900,label:'AIRLOCK',kind:'door'},{x:1450,label:'BLOCK 118',kind:'door'}]}}:null;}}};ctx.globalThis=ctx;vm.createContext(ctx);vm.runInContext(source,ctx);return {ctx,api:ctx.TechOpsGameplayExperience,frame(){if(rafFn)rafFn();}};}
let b=boot();assert.equal(b.api.VERSION,1);assert.equal(b.api.nextLandmark(b.ctx.TechOpsLevelRegistry.goodDogsMission(3),400).label,'AIRLOCK');assert.equal(b.api.nextLandmark(b.ctx.TechOpsLevelRegistry.goodDogsMission(3),1440).label,'BLOCK 118');assert.equal(b.api.nearestLandmark(b.ctx.TechOpsLevelRegistry.goodDogsMission(3),875).mark.label,'AIRLOCK');
b.ctx.NM={district:'downtown',x:100,w:24,enemies:[],_nightCombat:{events:[{id:1,type:'jab'},{id:2,type:'whiff'},{id:3,type:'hurt'}]}};b.ctx.S.nightMode=true;b.api.consumeCombat(b.ctx.NM);assert.ok(true,'semantic event consumption must not mutate combat authority');assert.equal(b.ctx.NM._nightCombat.events.length,3);assert.equal(b.ctx.NM._nightCombat.events[0].type,'jab');
assert.match(source,/never owns damage, collision, progression/);assert.match(source,/TechOpsLevelRegistry/);assert.match(source,/prefers-reduced-motion/);assert.match(source,/volSfx/);assert.doesNotMatch(source,/\.hp\s*[-+]=|\.alive\s*=|\.kills\s*\+\+|localStorage\.setItem/);
console.log('Production gameplay experience: PASS');
