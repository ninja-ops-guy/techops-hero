# Merge Gate Probe

Temporary validation file used to prove the repository-wide `Merge Gate / required` status appears on an otherwise documentation-only pull request and only passes when its upstream validation jobs succeed.
